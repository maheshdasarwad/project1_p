Way to start the project

First navigate to the backend folder
add 
# .env
  PORT = 
  MONGO_CONN=
  JWT_SECRET=

Then run following commands
1. npm install
2. npm start

Then open with frontend folder 
1. npm install
2. npm run dev
